<repoindex ttl="0"
    disturl="http://cdn.opensuse.org"
    distsub="leap-micro"
    distver="${releasever}"
    debugenable="false"
    sourceenable="false">

<repo url="%{disturl}/distribution/%{distsub}/%{distver}/product/repo/openSUSE-Leap-Micro-%{distver}-$DIST_ARCH?mediahandler=curl2"
    alias="repo-main"
    name="%{alias} (%{distver})"
    enabled="true"
    autorefresh="true"/>

<repo url="%{disturl}/distribution/%{distsub}/%{distver}/product/repo/openSUSE-Leap-Micro-%{distver}-$DIST_ARCH-Debug?mediahandler=curl2"
    alias="repo-debug"
    name="%{alias} (%{distver})"
    enabled="false"
    autorefresh="true"/>

<repo url="%{disturl}/distribution/%{distsub}/%{distver}/product/repo/openSUSE-Leap-Micro-%{distver}-$DIST_ARCH-Source?mediahandler=curl2"
    alias="repo-source"
    name="%{alias} (%{distver})"
    enabled="false"
    autorefresh="true"/>

</repoindex>
